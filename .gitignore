# Configuración de Entorno
.env
# .agent/ → trackeado en GitHub, excluido del host por --exclude en deploy-talento.yml
.gemini/

# Dependencias
node_modules/
# core/ → trackeado en GitHub, excluido del host por --exclude en deploy-talento.yml

# Archivos de Sistema / IDE
.vscode/
.idea/
.DS_Store
Thumbs.db

# Subidas (Carpeta dinámica no gestionada por Git)
uploads/

# Documentación Técnica (Mantenida en ERP o local)
# docs/ → trackeada en GitHub, excluida del host por --exclude en deploy-talento.yml

# Scripts de Deploy
# .scripts/ (Versionado en GitHub pero excluido del host)
